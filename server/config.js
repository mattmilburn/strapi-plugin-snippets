'use strict';

module.exports = {
  default: {
    contentTypes: null,
    ignoreUnmatched: false,
    uppercase: true,
  },
  validator: config => {
    // @TODO - Handle plugin config validation here.
  },
};
