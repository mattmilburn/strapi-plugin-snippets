'use strict';

module.exports = {
  default: {
    contentTypes: null,
    ignoreUnmatched: true,
    uppercase: true,
  },
  validator: config => {
    // @TODO - Handle plugin config validation here.
  },
};
