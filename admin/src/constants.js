import pluginId from './utils/plugin-id';

export const ACTION_RESOLVE_CONFIG = `${pluginId}/resolve-config`;

export const REGEX_SNIPPET_CODE = /^[a-zA-Z][a-zA-Z0-9_]+$/;

export const UID_SNIPPET = 'plugin::snippets.snippet';
